---
# 自定义UV-K5固件开发助手
description: 这是LOSEHU132系列UV-K5/K6对讲机固件的agent配置，用于指导AI助手进行固件修改和功能开发。
applyTo: "**/*.{c,h,md,ld,Makefile}"
---

# UV-K5 固件自定义开发指南

## 项目概述

基于 [egzumer/uv-k5-firmware-custom](https://github.com/egzumer/uv-k5-firmware-custom) 和 [DualTachyon](https://github.com/DualTachyon) 的开源固件，由 **LOSEHU** 整合修改的 Quansheng UV-K5/K6 对讲机自定义固件。

- **MCU**: DP32G030 (ARM Cortex-M0)
- **Flash**: 60KB (通过 firmware.ld 定义)
- **RAM**: 16KB (0x20000000 ~ 0x20004000)
- **EEPROM**: 原始 128KB / 已扩容 **4MiB**
- **射频芯片**: BK4819
- **显示**: ST7565 128x64 LCD

## 构建系统

### 编译选项 (Makefile)

关键编译选项在 `Makefile` 顶部定义，通过 `?=` 设置默认值：

| 选项 | 默认值 | 说明 |
|------|--------|------|
| `ENABLE_MDC1200` | **0** | ⚠️ **MDC1200信令，需要设为1** |
| `ENABLE_MDC1200_SHOW_OP_ARG` | 0 | 显示MDC操作码和参数 |
| `ENABLE_MDC1200_SIDE_BEEP` | 0 | MDC发送时的侧音提示 |
| `ENABLE_MDC1200_CONTACT` | 0 | MDC联系人功能 |
| `ENABLE_MDC1200_EDIT` | 0 | MDC ID编辑 |
| `ENABLE_UART` | 1 | 串口通信 |
| `ENABLE_VOX` | 1 | 声控发射 |
| `ENABLE_BIG_FREQ` | 1 | 大号频率显示 |
| `ENABLE_WIDE_RX` | 1 | 全频段接收(18-1300MHz) |
| `ENABLE_AM_FIX` | 1 | AM模式增益修复 |
| `ENABLE_SQUELCH_MORE_SENSITIVE` | 1 | 静噪更敏感 |
| `ENABLE_RSSI_BAR` | 1 | RSSI信号条 |
| `ENABLE_CUSTOM_SIDEFUNCTIONS` | 1 | 自定义侧键功能 |
| `ENABLE_SIDEFUNCTIONS_SEND` | 1 | 侧键发射功能 |
| `ENABLE_SCAN_RANGES` | 1 | 扫描范围 |
| `ENABLE_WARNING` | 1 | 蜂鸣提示音 |
| `ENABLE_DTMF_CALLING` | 1 | DTMF呼叫功能 |
| `ENABLE_FLASHLIGHT` | 1 | 手电筒功能 |
| `ENABLE_CTCSS_TAIL_PHASE_SHIFT` | **0** | CTCSS尾音相位偏移（原厂55Hz方式） |

### 版本命名

```
LOSEHU132         - 中文基础版 (无扩容需求)
LOSEHU132K        - 中文扩容版 (1MiB+ EEPROM)
LOSEHU132H        - 中文输入法版 (2MiB+ EEPROM)
LOSEHU132E        - 英文版
LOSEHU132EK       - 英文扩容版
LOSEHU132HS       - 中文SI4732版
```

用户当前编译的版本后缀通过 `PACKED_FILE_SUFFIX` 在 `version.c` 中定义。

### 构建命令

```bash
# Windows (需要msys2环境)
./win_make.bat

# 或者直接make
make ENABLE_MDC1200=1 ENABLE_CHINESE_FULL=4

# 使用Docker
./compile-with-docker.bat
```

## EEPROM 内存布局 (4MiB)

当前用户使用的是 **4MiB EEPROM**，有充足的空间。

| 地址范围 | 大小 | 内容 |
|---------|------|------|
| `0x00000~0x01CFF` | 标准 | 原厂频道/设置数据 |
| `0x01D00~0x01E00` | 256B | **MDC1200联系人**(前16个, 每个16B) |
| `0x01F90~0x01FF0` | 96B | **MDC1200联系人**(后6个, 每个16B) |
| `0x01FF8~0x01FFC` | 4B | 侧键功能配置 |
| `0x01FFD~0x01FFE` | 2B | **MDC1200 ID** |
| `0x01FFF` | 1B | MDC联系人数量 |
| `0x02000~0x02024` | - | 开机字符 |
| `0x02080~0x02480` | 1KB | 开机图片(128x64) |
| `0x02480~0x028B0` | - | 字体数据 |
| `0x028B0~0x02B96` | - | 菜单编码表 |
| `0x02E00~0x1E1E6` | ~109KB | **GB2312中文字库**(需4MiB) |
| `0x1E200~0x20000` | - | 多普勒卫星数据 |
| `0x20000~0x40000` | 128KB | 输入法/拼音数据等 |

## 尾音系统 (自定义尾音)

### 概述

尾音系统由 `settings.h` 中的 `ROGER_Mode_t` 枚举控制，通过菜单项 `MENU_ROGER` 配置，存储在 `gEeprom.ROGER`。

### ROGER模式枚举 (settings.h)

```c
enum ROGER_Mode_t {
    ROGER_MODE_OFF = 0,          // 关闭
    ROGER_MODE_ROGER,            // ROGER蜂鸣音
    ROGER_MODE_MDC_END,          // MDC尾音 (发射结束时发送MDC)
    ROGER_MODE_MDC_HEAD,         // MDC首音 (发射开始时发送MDC)
    ROGER_MODE_MDC_BOTH,         // MDC首尾音 (发射开始和结束都发送MDC)
    ROGER_MODE_MDC_HEAD_ROGER    // MDC首音 + ROGER尾音
};
```

### 尾音触发流程

1. **发射结束时** (`RADIO_SendEndOfTransmission` in `radio.c:65`):
   - 调用 `BK4819_PlayRoger()` - 播放ROGER/MDC尾音
   - 调用 `DTMF_SendEndOfTransmission()` - DTMF下线码
   - 如果 `gEeprom.TAIL_TONE_ELIMINATION == true`，调用 `RADIO_SendCssTail()`
   - 最后 `RADIO_SetupRegisters(false)` 切回接收

2. **发射开始时** (`FUNCTION_Transmit` in `functions.c:207`):
   - 如果 `ROGER == MDC_HEAD / MDC_BOTH / MDC_HEAD_ROGER`
   - 发送 `MDC1200 PTT ID` (op=1, arg=0x80)

3. **BK4819_PlayRoger()** (`driver/bk4819.c:329`):
   - `ROGER_MODE_ROGER` / `ROGER_MODE_MDC_HEAD_ROGER` → 播放蜂鸣音 `BK4819_PlayRogerNormal()`
   - `ROGER_MODE_MDC_END` / `ROGER_MODE_MDC_BOTH` → 发送 `MDC1200 POST_ID` (op=1, arg=0x00)

### 尾音消除 (Tail Tone Elimination)

`gEeprom.TAIL_TONE_ELIMINATION` 控制接收结束时是否发送CSS尾音以消除静噪尾音。

- **CTCSS尾音**: `BK4819_PlayCTCSSTail()` - 通过 `BK4819_GenTail(4)` 发送55Hz音调（原厂方式）
  - 如果启用 `ENABLE_CTCSS_TAIL_PHASE_SHIFT`，则改用180°相位偏移方式（更兼容）
- **CDCSS尾音**: `BK4819_PlayCDCSSTail()` - 发送134.4Hz尾音

### 自定义尾音的修改点

如果要**自定义尾音**（ROGER蜂鸣音），修改以下位置：

1. **`driver/bk4819.c`** 中的 `BK4819_PlayRogerNormal()` 函数 (约第1410行):
   - 修改 `tone1_Hz` 和 `tone2_Hz` 频率
   - 修改音调时长 `SYSTEM_DelayMs(80)`
   - 修改音调增益 (REG_70中的TONE1_TUNING_GAIN)
   - 当前默认为摩托罗拉风格: 1540Hz + 1310Hz 双音

2. **增加新的ROGER模式**:
   - 在 `settings.h` 的 `ROGER_Mode_t` 枚举中添加新模式
   - 在 `BK4819_PlayRoger()` 中添加对应处理
   - 在 `ui/menu.c` 的 `gSubMenu_ROGER` 数组中添加显示文字
   - 在 `app/menu.c` 中添加保存逻辑
   - 更新 `settings.c` 中的默认值范围检查

3. **使用自定义音频数据替代蜂鸣音**:
   - 可以通过 `BK4819_PlaySingleTone()` 播放单音
   - 也可以通过 `BK4819_start_tone()` / `BK4819_stop_tones()` 控制更复杂的音调序列
   - 如果需要播放WAV/音频采样，需要自己实现PCM到FM调制的转换

## MDC1200 信令系统

### 启用方式

在 `Makefile` 中将以下选项设为 **1**:
```makefile
ENABLE_MDC1200                = 1    # 必须开启
ENABLE_MDC1200_SHOW_OP_ARG    = 0    # 显示接收到的OP/ARG
ENABLE_MDC1200_SIDE_BEEP      = 1    # 发送MDC时播放侧音
ENABLE_MDC1200_CONTACT        = 1    # MDC联系人功能
ENABLE_MDC1200_EDIT           = 1    # MDC ID编辑
```

### 核心文件

| 文件 | 说明 |
|------|------|
| `app/mdc1200.c` | MDC1200编解码核心 (卷积编码、交织、FEC纠错) |
| `app/mdc1200.h` | 头文件，操作码定义 |
| `driver/bk4819.c` (BK4819_send_MDC1200) | 通过BK4819的FSK调制发送MDC数据包 |
| `settings.h` | MDC1200_ID 存储 (uint32_t) |

### 协议格式

MDC1200数据包: `Preamble(3B) + Sync(5B) + OP(1B) + ARG(1B) + ID(2B) + CRC(2B) + Status(1B) + FEC(7B)`

操作码:
- `0x01 (0x80)` = PTT ID (发射开始)
- `0x01 (0x00)` = POST ID (发射结束)
- `0x11 (0x8A)` = 远程监控
- `0x35 (0x89)` = 呼叫提醒
- `0x63 (0x85)` = 无线电检查

### EEPROM中的MDC联系人

- 存储地址: `0x01D00~0x01E00` (前16个) + `0x01F90~0x01FF0` (后6个)
- 每个联系人: 2B MDC ID + 14B 名称
- 联系人数量: `0x01FFF`
- MDC ID: `0x01FFD~0x01FFE`

## 电池系统

### 电池类型

```c
typedef enum {
    BATTERY_TYPE_1600_MAH,   // 1600mAh 电池
    BATTERY_TYPE_2200_MAH,   // 2200mAh 电池
    BATTERY_TYPE_UNKNOWN
} BATTERY_Type_t;
```

存储在 `gEeprom.BATTERY_TYPE`，通过菜单 `MENU_BATTYP` 设置。

### 放电曲线表 (`helper/battery.c`)

当前固件的电压→百分比映射表 (电压单位: 10mV):

```
1600mAh电池:
  电压(mV)  | 电池百分比
  8280      | 100% (充满)
  8140      |  97%
  7600      |  25%
  7290      |   6%
  6300      |   0% (关机)

2200mAh电池:
  电压(mV)  | 电池百分比
  8320      | 100%
  8130      |  95%
  7400      |  60%
  7070      |  21%
  6820      |   5%
  6300      |   0%
```

### 建议的1600mAh更精细放电曲线

由于你有 **4MiB EEPROM**，有充足空间可以扩展放电曲线。建议修改 `helper/battery.c` 中的 `Voltage2PercentageTable`，添加更多采样点以获得更精确的百分比显示：

```c
const uint16_t Voltage2PercentageTable[][7][2] = {
    [BATTERY_TYPE_1600_MAH] = {
        {828, 100},  // 8.28V - 满电
        {822,  98},  // 8.22V
        {816,  93},  // 8.16V
        {808,  80},  // 8.08V
        {798,  60},  // 7.98V
        {788,  45},  // 7.88V - 中等电量
        {776,  32},  // 7.76V
        {765,  22},  // 7.65V
        {754,  15},  // 7.54V
        {743,  10},  // 7.43V
        {730,   6},  // 7.30V - 低压警告
        {710,   3},  // 7.10V
        {690,   1},  // 6.90V
        {630,   0},  // 6.30V - 关机
    },
    // ...2200mAh保持不变
};
```

> ⚠️ 注意: `Voltage2PercentageTable` 的第二维大小改为14（而非7），需要同时修改 `static_assert` 和 `BATTERY_VoltsToPercent` 中的循环条件。

### 电池显示等级 (`BATTERY_GetReadings`)

```
电压 > 8.90V   → 7格 (过压)
电压 8.28~8.90 → 百分比映射到1~6格
  百分比 > 88% → 6格
  百分比 > 65% → 5格
  百分比 > 41% → 4格
  百分比 > 17% → 3格
  百分比 >  5% → 2格
  百分比 ≤  5% → 1格
电压 < 6.30V   → 0格 (严重低电)
```

### 低电告警

- 当显示等级 < 2 时触发低电标志
- 500ms循环闪烁电池图标
- 30个周期后（15秒）播放双蜂鸣警告
- 如果持续低电则进入 `FUNCTION_POWER_SAVE` 省电模式

## 代码修改指南

### 修改尾音的自定义实现

**方案A - 修改现有的ROGER蜂鸣音频率/时长:**
编辑 `driver/bk4819.c` 中的 `BK4819_PlayRogerNormal()` 函数。

**方案B - 添加自定义尾音播放函数:**
1. 在 `driver/bk4819.h` 中声明新函数 `void BK4819_PlayCustomTail(void);`
2. 在 `driver/bk4819.c` 中实现，使用 `BK4819_start_tone()` / `BK4819_stop_tones()` 或直接操作BK4819寄存器
3. 在 `BK4819_PlayRoger()` 中调用你的自定义函数

**方案C - 完全替换尾音系统:**
如果你想要完全自己控制尾音发送逻辑，可以:
1. 修改 `radio.c` 中的 `RADIO_SendEndOfTransmission()` - 去掉 `BK4819_PlayRoger()` 调用
2. 修改 `functions.c` 中的 `FUNCTION_Transmit()` - 去掉MDC首音发送
3. 在 `app/app.c` 中的 `HandleReceive()` 处添加你自己的尾音处理逻辑
4. 或者新建一个文件 `custom_tail.c` / `custom_tail.h` 来组织代码

### 添加新功能的通用步骤

1. **Makefile**: 添加新的编译选项（`ENABLE_MY_FEATURE ?= 0`）
2. **添加CFLAGS**: 在Makefile中添加 `CFLAGS += -DENABLE_MY_FEATURE`
3. **条件编译**: 使用 `#ifdef ENABLE_MY_FEATURE` 包裹新代码
4. **菜单项**: 在 `ui/menu.h` 的枚举中添加新菜单项
5. **菜单UI**: 在 `ui/menu.c` 中添加显示文字
6. **菜单逻辑**: 在 `app/menu.c` 的 `HandleMenu()` / `MenuDisplay()` 中添加处理
7. **EEPROM存储**: 在 `settings.h` 的 `EEPROM_Config_t` 结构体中添加字段
8. **EEPROM读写**: 在 `settings.c` 的 `SETTINGS_InitEEPROM()` 和 `SETTINGS_SaveSettings()` 中读写

### 菜单操作流程 (关键代码追踪)

```
按键扫描 → app/action.c → app/generic.c → 
  app/menu.c (HandleMenu键盘处理) → 
  ui/menu.c (MenuDisplay显示处理) → 
  settings.c (存储到EEPROM)
```

## 关键文件索引

| 文件 | 作用 |
|------|------|
| `main.c` | 主入口，初始化调用 |
| `Makefile` | 构建配置，所有编译选项 |
| `firmware.ld` | 链接脚本，FLASH/RAM布局 |
| `settings.h` | EEPROM配置结构体定义 |
| `settings.c` | EEPROM读写实现 |
| `radio.h` | VFO信息结构体、调制模式定义 |
| `radio.c` | 射频控制，发射/接收切换 |
| `functions.c` | 功能状态机 (发射/接收/省电等) |
| `app/app.c` | 应用层逻辑 (接收处理、10ms定时等) |
| `app/menu.c` | 菜单按键处理 |
| `ui/menu.c` | 菜单显示、文字定义 |
| `ui/menu.h` | 菜单项枚举 |
| `app/mdc1200.c` | MDC1200编解码 |
| `app/mdc1200.h` | MDC1200操作码定义 |
| `driver/bk4819.c` | BK4819射频芯片驱动 (核心) |
| `driver/bk4819.h` | BK4819驱动头文件 |
| `helper/battery.c` | 电池电压检测和百分比计算 |
| `helper/battery.h` | 电池类型和接口定义 |
| `driver/eeprom.c` | EEPROM读写底层 |
| `driver/st7565.c` | LCD驱动 |
| `chinese.h` | 中文字符串定义 |
| `misc.h` | 通用宏定义和常量 |
| `board.c` | 板级初始化 |

## 注意事项

1. **Flash只有60KB**，代码需要精打细算。添加功能时注意 `arm-none-eabi-size firmware.elf` 的输出
2. **RAM只有16KB**，全局变量和栈空间有限
3. 修改EEPROM布局时，注意与写频软件的兼容性
4. MDC1200代码已在启用时通过 `#ifdef ENABLE_MDC1200` 保护，不会增加Flash占用
5. 4MiB EEPROM有大量空闲空间（0x2E000~0x40000），可以存放用户数据
6. BK4819芯片通过SPI-like接口控制，直接读写寄存器即可实现自定义功能
7. 需要经过充分测试再刷写到对讲机上，建议先使用模拟器或仿真器验证

## RTC实时时钟功能 (ENABLE_TIMER)

### 功能说明
利用DP32G030内置的RTC硬件（32768Hz RCLF晶振），实现了日期时间保持和显示功能。

### 已修改的文件

| 文件 | 修改内容 |
|------|---------|
| `Makefile` | `ENABLE_TIMER ?= 1`（默认启用） |
| `driver/rtc.c` | 添加`RTCHandler`中断服务（每秒读RTC）、时间有效性校验、`RTC_SaveToEEPROM()`、`gRTC_SecondTick`标志 |
| `bsp/dp32g030/rtc.h` | 添加`RTC_SaveToEEPROM()`声明和`gRTC_SecondTick`外部变量 |
| `main.c` | 添加`RTC_INIT()`调用（非doppler模式）、添加rtc.h包含 |
| `app/app.c` | 在`APP_TimeSlice500ms()`中检查`gRTC_SecondTick`并更新状态栏 |
| `ui/status.c` | 在状态栏添加`HH:MM`时间显示（VOX图标之后） |
| `ui/menu.h` | 添加`MENU_RTC_SET`菜单项枚举 |
| `ui/menu.c` | 添加菜单列表项"时间设置"、显示当前时间`HH:MM:SS` |
| `app/menu.c` | 添加数字输入处理（6位HHMMSS格式）、保存到RTC和EEPROM |
| `chinese.h` | 所有3个编码版本添加"时间设置"文本 |

### 使用方法

1. **查看时间**：状态栏上直接显示 `HH:MM` 格式的当前时间
2. **设置时间**：进入菜单 → 找到"RTC Set" → 按F进入编辑 → 输入6位数字（HHMMSS）→ 按EXIT保存
3. **时间持久化**：每10分钟自动保存到EEPROM(地址0x2BC0)，断电不丢失

### EEPROM存储

- 地址：`0x2BC0`（6字节：年、月、日、时、分、秒）
- 格式：BCD-like（例如 0x26, 0x05, 0x24, 0x14, 0x30, 0x00 = 2026-05-24 14:30:00）

### 禁用方法

在`Makefile`中将`ENABLE_TIMER ?= 1`改回`ENABLE_TIMER ?= 0`

## 自定义尾音指南

### BK4819的音调生成能力

BK4819射频芯片可以生成多种音调：
- **单音**: `BK4819_PlaySingleTone(freq_Hz, delay_ms, level, play_speaker)`
- **双音**: 通过寄存器REG_70/71控制TONE1/TONE2
- **DTMF**: `BK4819_PlayDTMF(char Code)` 
- **自定义频率**: `BK4819_start_tone(frequency, level, tx, tx_mute)`

⚠️ **注意**: BK4819是射频收发芯片，**无法直接播放WAV/MP3音频文件**。它只能生成特定频率的正弦波音调。

### 方案A - 修改现有ROGER蜂鸣音（最简单）

编辑 `driver/bk4819.c` 中的 `BK4819_PlayRogerNormal()` 函数（约第1410行）：

```c
void BK4819_PlayRogerNormal(void) {
    // 修改这两个频率来改变音调
    const uint32_t tone1_Hz = 1540;  // 第一个音调频率(Hz)
    const uint32_t tone2_Hz = 1310;  // 第二个音调频率(Hz)
    
    // ... 修改延时来控制时长
    SYSTEM_DelayMs(80);  // 音调持续时间
}
```

### 方案B - 创建音调序列（推荐，用 wav2tail.py 自动生成）

项目提供了 `tools/wav2tail.py` 工具，可以自动分析WAV音频并生成C代码：

```bash
# 安装依赖
pip install numpy

# 分析WAV并生成自定义尾音
python tools/wav2tail.py your_audio.wav -o driver/custom_tail.c

# 指定更多音调数(默认8)
python tools/wav2tail.py your_audio.wav --tones 12

# 限制频率范围
python tools/wav2tail.py your_audio.wav --min-freq 500 --max-freq 2500
```

集成步骤：
1. 运行 `python tools/wav2tail.py your_audio.wav`
2. 生成的 `driver/custom_tail.c` 已包含完整代码
3. 在 `driver/bk4819.c` 的 `BK4819_PlayRoger()` 中添加自定义尾音调用
4. 在 `Makefile` 的 `OBJS` 中添加: `OBJS += driver/custom_tail.o`

**新增文件：**
- `driver/custom_tail.c` - 自定义尾音播放模块（已创建，包含示例音调）
- `tools/wav2tail.py` - WAV→尾音分析转换工具
- `BK4819_PlayCustomTail()` 已在 `driver/bk4819.h` 中声明

```c
// 示例：播放一个简单的三音序列
void BK4819_PlayCustomTail(void) {
    BK4819_EnterTxMute();
    BK4819_SetAF(BK4819_AF_MUTE);
    BK4819_WriteRegister(BK4819_REG_70, BK4819_REG_70_ENABLE_TONE1 | (66u << BK4819_REG_70_SHIFT_TONE1_TUNING_GAIN));
    BK4819_EnableTXLink();
    SYSTEM_DelayMs(50);
    
    // 音调1: 1000Hz
    BK4819_WriteRegister(BK4819_REG_71, scale_freq(1000));
    BK4819_ExitTxMute();
    SYSTEM_DelayMs(100);
    
    // 音调2: 1500Hz  
    BK4819_WriteRegister(BK4819_REG_71, scale_freq(1500));
    SYSTEM_DelayMs(100);
    
    // 音调3: 2000Hz
    BK4819_WriteRegister(BK4819_REG_71, scale_freq(2000));
    SYSTEM_DelayMs(100);
    
    BK4819_EnterTxMute();
    BK4819_TurnsOffTones_TurnsOnRX();
}
```

### 方案C - 完全替换尾音逻辑

如果你想完全自己控制，可以：

1. 修改 `radio.c` 中的 `RADIO_SendEndOfTransmission()` - 移除 `BK4819_PlayRoger()` 调用
2. 修改 `functions.c` 中的 `FUNCTION_Transmit()` - 移除MDC首音
3. 创建新的 `custom_tail.c` / `custom_tail.h` 文件实现自定义逻辑

### 添加新的ROGER模式步骤

1. 在 `settings.h` 的 `ROGER_Mode_t` 枚举中添加新模式
2. 在 `driver/bk4819.c` 的 `BK4819_PlayRoger()` 中添加处理
3. 在 `ui/menu.c` 的 `gSubMenu_ROGER` 数组中添加显示文字
4. 在 `chinese.h` 的3个编码段添加对应文字
5. 更新 `settings.c` 中的范围检查

## 1600mAh电池放电曲线优化

### 当前曲线（6个采样点）

```c
const uint16_t Voltage2PercentageTable[][7][2] = {
    [BATTERY_TYPE_1600_MAH] = {
        {828, 100},  // 8.28V → 100%
        {814,  97},  // 8.14V →  97%
        {760,  25},  // 7.60V →  25%
        {729,   6},  // 7.29V →   6%
        {630,   0},  // 6.30V →   0%
        {0,     0},
        {0,     0},
    },
    // ...
};
```

### 建议的精细曲线（14个采样点）

由于你有4MiB大容量EEPROM，有足够的Flash空间存储更多采样点：

```c
static const uint16_t Voltage2PercentageTable[][14][2] = {
    [BATTERY_TYPE_1600_MAH] = {
        {828, 100},  // 8.28V 满电
        {823,  98},  // 8.23V 
        {818,  94},  // 8.18V
        {812,  85},  // 8.12V
        {805,  72},  // 8.05V
        {797,  56},  // 7.97V 
        {788,  42},  // 7.88V 中等电量
        {778,  30},  // 7.78V
        {768,  22},  // 7.68V
        {756,  15},  // 7.56V
        {743,  10},  // 7.43V
        {728,   6},  // 7.28V 低压警告
        {710,   3},  // 7.10V
        {690,   1},  // 6.90V
        {630,   0},  // 6.30V 关机
    },
    [BATTERY_TYPE_2200_MAH] = {
        // ...保持2200mAh不变
    },
};
```

**修改此表需要同时更新以下位置：**
- 数组第二维大小（`[7]` → `[15]`）
- `static_assert` 中的大小检查
- `BATTERY_VoltsToPercent()` 函数中的循环上界

## 修改记录

| 日期 | 修改内容 | 涉及文件 |
|------|---------|---------|
| 2026-05-24 | 创建agent.md | `.agent.md` |
| 2026-05-24 | 启用ENABLE_TIMER=1（RTC时钟功能） | `Makefile` |
| 2026-05-24 | 添加RTCHandler中断服务（每秒读RTC、每10分钟保存EEPROM） | `driver/rtc.c` |
| 2026-05-24 | 添加gRTC_SecondTick标志和RTC_SaveToEEPROM接口 | `bsp/dp32g030/rtc.h` |
| 2026-05-24 | 添加RTC_INIT()开机初始化（非doppler模式） | `main.c` |
| 2026-05-24 | 500ms定时中更新状态栏（时间变化时刷新） | `app/app.c` |
| 2026-05-24 | 状态栏添加HH:MM时间显示 | `ui/status.c` |
| 2026-05-24 | 添加MENU_RTC_SET菜单项 | `ui/menu.h` |
| 2026-05-24 | 添加菜单列表"时间设置"和HH:MM:SS显示 | `ui/menu.c` |
| 2026-05-24 | 添加6位数字输入设置时间(HHMMSS) | `app/menu.c` |
| 2026-05-24 | 添加"时间设置"中文文本（3个编码版本） | `chinese.h` |
| 2026-05-24 | 创建wav2tail.py WAV→尾音分析工具 | `tools/wav2tail.py` |
| 2026-05-24 | 创建custom_tail.c自定义尾音播放模块 | `driver/custom_tail.c` |
| 2026-05-24 | 添加BK4819_PlayCustomTail()函数声明 | `driver/bk4819.h` |
| 2026-05-24 | 集成custom_tail到尾音系统(BK4819_PlayRoger→PlayCustomTail) | `driver/bk4819.c` |
| 2026-05-24 | 添加custom_tail.o到编译列表 | `Makefile` |


